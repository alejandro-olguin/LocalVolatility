% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{american_option_heston}
\alias{american_option_heston}
\title{American Option (Heston stochastic volatility, penalty method)}
\usage{
american_option_heston(
  s_0,
  k,
  tau,
  r_d,
  q,
  kappa,
  theta,
  xi,
  rho,
  v_0,
  type,
  s_min,
  s_max,
  v_min,
  v_max,
  n_s,
  n_v,
  n_t,
  alpha,
  lambda,
  tolerance
)
}
\arguments{
\item{s_0}{Stock spot price (> 0).}

\item{k}{Strike price (> 0).}

\item{tau}{Time to expiry in years (> 0).}

\item{r_d}{Risk-free rate.}

\item{q}{Dividend yield.}

\item{kappa}{Mean reversion speed (> 0).}

\item{theta}{Long-run variance (> 0).}

\item{xi}{Volatility of variance (vol-of-vol, > 0).}

\item{rho}{Correlation between stock and variance in \code{[-1, 1]}.}

\item{v_0}{Initial variance (> 0).}

\item{type}{Either \code{"call"} or \code{"put"}.}

\item{s_min, s_max}{Min/Max of the stock grid.}

\item{v_min}{Min of the variance grid (must be > 0, e.g. 0.001).}

\item{v_max}{Max of the variance grid.}

\item{n_s}{Number of intervals in stock grid (\code{n_s + 1} nodes, >= 3).}

\item{n_v}{Number of intervals in variance grid (\code{n_v + 1} nodes, >= 3).}

\item{n_t}{Number of time steps (>= 1).}

\item{alpha}{Grid clustering parameter for Tavella-Randall grids (> 0).}

\item{lambda}{Penalty parameter (> 0, typically 1e4 to 1e6).}

\item{tolerance}{Convergence tolerance for penalty iterations.}
}
\value{
Option price as a numeric scalar.
}
\description{
Prices an American option under the Heston stochastic volatility model
using a 2D finite-difference PDE with Yanenko operator splitting and a
penalty-projection scheme for the early exercise constraint.
}
\details{
Solves the Heston LCP \eqn{\min(-\mathcal{L}V, V - \phi) = 0} where
the differential operator is:
\deqn{\mathcal{L}V = V_t + \frac{1}{2}vS^2 V_{SS} + \rho\xi v S V_{Sv}
  + \frac{1}{2}\xi^2 v V_{vv} + (r_d - q)S V_S
  + \kappa(\theta - v) V_v - r_d V.}
The penalty parameter is split as \eqn{\lambda/2} across the two
operator-splitting sub-steps. Maximum 200 penalty iterations per time
step with a warning on non-convergence.
}
\examples{
american_option_heston(100, 100, 1, 0.05, 0, 2, 0.04, 0.5, -0.7, 0.04,
                       "put", 20, 300, 0.001, 1.0, 80, 40, 100, 3,
                       1e4, 1e-8)

}
