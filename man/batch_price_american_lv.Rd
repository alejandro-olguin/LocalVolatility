% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{batch_price_american_lv}
\alias{batch_price_american_lv}
\title{Batch-price American options under local volatility (1D)}
\usage{
batch_price_american_lv(
  spots,
  strikes,
  taus,
  r_ds,
  qs,
  sigma,
  types,
  s_min,
  s_max,
  n_s,
  n_t,
  lambda,
  tolerance
)
}
\arguments{
\item{spots}{Numeric vector of spot prices (one per option).}

\item{strikes}{Numeric vector of strike prices.}

\item{taus}{Numeric vector of times to expiry (years).}

\item{r_ds}{Numeric vector of domestic risk-free rates.}

\item{qs}{Numeric vector of dividend yields.}

\item{sigma}{Local volatility matrix of size \code{(n_s + 1) x (n_t + 1)}.}

\item{types}{Character vector, each element \code{"call"} or \code{"put"}.}

\item{s_min, s_max}{Domain bounds for the asset grid.}

\item{n_s}{Number of spatial intervals (\code{n_s + 1} nodes).}

\item{n_t}{Number of time steps.}

\item{lambda}{Penalty parameter (> 0, typically 1e4 to 1e6).}

\item{tolerance}{Convergence tolerance for penalty iterations.}
}
\value{
Numeric vector of option prices (same length as \code{strikes}).
}
\description{
Prices multiple American options that share the same local volatility surface
and PDE grid. Each option is an independent backward Crank-Nicolson solve
with penalty-projection for early exercise, parallelized across options
via OpenMP.
}
\details{
Each option is solved independently using the same sigma surface and grid.
The solver uses Crank-Nicolson with a penalty-projection fixed-point
iteration for the American constraint, capped at 200 iterations per time
step. Options are solved in parallel across CPU cores using std::thread.
}
