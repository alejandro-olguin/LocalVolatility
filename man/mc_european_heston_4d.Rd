% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mc_european_heston_4d}
\alias{mc_european_heston_4d}
\title{European Option 4D Monte Carlo (Double-Heston Quanto)}
\usage{
mc_european_heston_4d(
  s_0,
  x_0,
  k,
  tau,
  r_d,
  r_f,
  q,
  kappa_s,
  theta_s,
  xi_s,
  v_s0,
  kappa_x,
  theta_x,
  xi_x,
  v_x0,
  rho_sx,
  rho_sv,
  rho_xv,
  rho_svx,
  rho_xvs,
  rho_vsvx,
  type,
  n_paths,
  n_steps,
  seed,
  n_threads = 0L
)
}
\arguments{
\item{s_0}{Equity spot price (> 0).}

\item{x_0}{FX spot price, domestic per foreign (> 0).}

\item{k}{Strike price in domestic currency (> 0).}

\item{tau}{Time to expiry in years (> 0).}

\item{r_d}{Domestic risk-free rate.}

\item{r_f}{Foreign risk-free rate.}

\item{q}{Equity dividend yield.}

\item{kappa_s}{Mean reversion speed for equity variance (> 0).}

\item{theta_s}{Long-run equity variance (> 0).}

\item{xi_s}{Vol-of-vol for equity (> 0).}

\item{v_s0}{Initial equity variance (> 0).}

\item{kappa_x}{Mean reversion speed for FX variance (> 0).}

\item{theta_x}{Long-run FX variance (> 0).}

\item{xi_x}{Vol-of-vol for FX (> 0).}

\item{v_x0}{Initial FX variance (> 0).}

\item{rho_sx}{Correlation between S and X.}

\item{rho_sv}{Correlation between S and v_S.}

\item{rho_xv}{Correlation between X and v_X.}

\item{rho_svx}{Correlation between S and v_X (typically ~0).}

\item{rho_xvs}{Correlation between X and v_S (typically ~0).}

\item{rho_vsvx}{Correlation between v_S and v_X (typically ~0).}

\item{type}{Either \code{"call"} or \code{"put"}.}

\item{n_paths}{Number of Monte Carlo paths (e.g. 1e6).}

\item{n_steps}{Number of time steps per path (e.g. 100).}

\item{seed}{RNG seed for reproducibility.}

\item{n_threads}{Number of threads to use (0 = auto-detect via
\code{std::thread::hardware_concurrency()}; default 0). Set to 1 for
fully reproducible results across machines.}
}
\value{
A list with components \code{price} (option price) and
\code{std_error} (Monte Carlo standard error).
}
\description{
Prices a European option on a foreign equity quoted in domestic currency
under double Heston stochastic volatility (one Heston process for the
equity, one for the FX rate) using Monte Carlo simulation with
\code{std::thread} parallelism and antithetic variates.
}
\details{
Simulates the 4D system (S, X, v_S, v_X) under the domestic measure using
Euler-Maruyama with full truncation for variance processes. Correlated
Brownian motions are generated via Cholesky decomposition of the 6-parameter
correlation matrix. Antithetic variates halve the variance at no extra
simulation cost.

The domestic-measure dynamics are:
\deqn{dS = (r_f - q - \rho_{SX}\sqrt{v_S}\sqrt{v_X}) S \, dt + \sqrt{v_S} S \, dW_S}
\deqn{dX = (r_d - r_f) X \, dt + \sqrt{v_X} X \, dW_X}
\deqn{dv_S = \kappa_S(\theta_S - v_S) \, dt + \xi_S \sqrt{v_S} \, dW_{v_S}}
\deqn{dv_X = \kappa_X(\theta_X - v_X) \, dt + \xi_X \sqrt{v_X} \, dW_{v_X}}
}
\examples{
mc_european_heston_4d(100, 20, 2000, 1, 0.05, 0.02, 0.01,
  2, 0.04, 0.5, 0.04, 1.5, 0.02, 0.3, 0.02,
  0.3, -0.7, -0.5, 0, 0, 0, "call", 100000, 100, 42)

}
